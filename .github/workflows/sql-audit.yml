name: Auditoría SQL (NOLOCK & reglas)

on:
  push:
    paths:
      - '**/*.sql'
      - '.github/audit/**'
      - '.github/workflows/sql-audit.yml'
  pull_request:
    paths:
      - '**/*.sql'
      - '.github/audit/**'
      - '.github/workflows/sql-audit.yml'

jobs:
  audit-sql:
    runs-on: ubuntu-latest
    steps:
      - name: 🧾 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 🔍 Ejecutar auditoría
        run: python .github/audit/audit_sql_standards.py
